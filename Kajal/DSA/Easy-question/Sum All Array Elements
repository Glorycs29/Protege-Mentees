https://www.geeksforgeeks.org/problems/sum-all-array-elements/1?page=4&category=CPP&sortBy=submissions

// User function Template for C++

int arraySum(int arr[], int size) {
    // Just return the sum of the list
    // Don't print here
    int sum = 0;
    for(int i=0; i<size; i++){
        sum = sum + arr[i];
    }
    return sum;
}

Time Complexity: O(n)
Auxiliary Space: O(1)
