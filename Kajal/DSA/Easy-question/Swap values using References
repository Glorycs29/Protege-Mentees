#include <iostream>
using namespace std;

// Function to swap using reference
void swapNumbers(int &a, int &b) {
    int temp = a;
    a = b;
    b = temp;
}

int main() {
    int a, b;
    cin >> a >> b;

    swapNumbers(a, b);  // Passing by reference
    cout << a << " " << b << endl;

    return 0;
}
