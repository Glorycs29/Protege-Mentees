#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    int sum = (n * (n + 1)) / 2;  // Using the formula for sum of first n natural numbers
    cout << sum << endl;

    return 0;
}
