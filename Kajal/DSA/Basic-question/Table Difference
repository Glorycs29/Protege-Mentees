#include <iostream>
using namespace std;

int main() {
    int n1, n2;
    cin >> n1 >> n2;

    for (int i = 1; i <= 10; i++) {
        cout << (n1 * i - n2 * i);
        if (i != 10) cout << " "; // avoid space after the last number
    }

    return 0;
}
